using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Quaternion a = Quaternion.FromToRotation(Vector3.forward, Vector3.right);
        //Quaternion b = a.normalized;
        //Debug.Log("Angle: " + Quaternion.Angle(Quaternion.identity, a));
        //Debug.Log("Normalized angle: " + Quaternion.Angle(Quaternion.identity, b));
        //Debug.Log("Normalized angle in rad: " + Quaternion.Angle(Quaternion.identity, b) * Mathf.Deg2Rad);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
